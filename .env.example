# === Tango Agent - Variaveis do OpenClaw ===
# Este arquivo e lido pelo systemd (EnvironmentFile) e pelos scripts.
# NAO coloque credenciais de infra (Hetzner, etc) aqui.

# Gateway Security (OBRIGATORIO)
OPENCLAW_GATEWAY_TOKEN=CHANGE_ME

# LLM Provider (pelo menos um)
ANTHROPIC_API_KEY=sk-ant-...
# OPENAI_API_KEY=sk-...

# Telegram
TELEGRAM_BOT_TOKEN=123456:ABCDEF...
TELEGRAM_USER_ID=

# Dominio (necessario para HTTPS via Caddy)
# DOMAIN=tango.seudominio.com

# Paths (nao alterar salvo necessidade)
OPENCLAW_CONFIG_DIR=./data/config
OPENCLAW_WORKSPACE_DIR=./data/workspace

# === Projetos Git (agentes coding) ===
# Diretorio de projetos acessivel pelos agentes coding
# VPS: /home/deploy/projects   Local: ./projects
PROJECTS_DIR=./projects

# Git config (identidade para commits dos agentes)
GIT_USER_NAME=Lucas Torres
GIT_USER_EMAIL=

# GitHub Personal Access Token (para git push via HTTPS)
# Criar em: https://github.com/settings/tokens (scope: repo)
# GIT_TOKEN=ghp_...

# === Google Workspace (gog) ===
# Senha para o keyring do gog
# GOG_KEYRING_PASSWORD=uma-senha-segura
# Conta Google padrao (evita repetir --account)
# GOG_ACCOUNT=sua-conta@gmail.com

# === Tango Bot (Agent SDK) ===
# Auth alternativa (Max Pro billing, opcional — se ambos existem, ANTHROPIC_API_KEY tem prioridade)
# CLAUDE_CODE_OAUTH_TOKEN=eyJ...
